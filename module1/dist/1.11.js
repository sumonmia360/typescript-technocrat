"use strict";
{
    // Deffarent types of ? mark symbol using use case
    // tarnary operator
    // optional type,
    // optional chaning,
    // nullish coalescing
    const age = 24;
    if (age >= 30) {
        console.log("youger");
    }
    else {
        console.log("middelAger");
    }
}
